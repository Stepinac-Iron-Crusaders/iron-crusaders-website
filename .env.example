# Frontend — where the API lives
# For local dev with `npm run dev` + `npm run dev` in server/, leave as /api (proxied)
# For production if you deploy the API separately (Render/Railway/Fly), set the full URL:
# VITE_API_URL=https://your-api.onrender.com
VITE_API_URL=
